let first = "Knowledge is power but enthusiasm pulls the switch";
let second = "Do or do not. There is no try";
let third  = "4,8,15,16,23,42";

/* let mySplit = third.split(","); //It splits the string where it finds the given value and puts it all into an array.
console.log(mySplit);

let mySlice = first.slice(0,10);
console.log(mySlice);
 */

/*  let mySubstring = first.substr(0,10);
 console.log(mySubstring);

 let myEndswith = second.endsWith(`try`); // It checks if the final of a string is the value given
 console.log(myEndswith);

 let myStartswith = second.startsWith(`do`); // It checks if the start of a string is the value given
 console.log(myStartswith);

 let myInclude = second.includes("do"); // Checks if the string contains this value 
 console.log(myInclude); */

 let myRepeat = `Ha!  `.repeat(10); // It repeats the string as much as we want.
 console.log(myRepeat);

 let myTrim = `      Bloated     `;
 console.log(myTrim.length);
 console.log(myTrim.trim().length); // Removes all the Spaces